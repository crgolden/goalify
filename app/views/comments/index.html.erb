<%= content_for :title, 'Goalify > All Comments' %>
<% if @comments.present? %>
    <%= render layout: 'layouts/panel', locals: {title: 'Viewing Goal Comments', resource: @comments} do %>
        <%= render 'goals/comments' %>
    <% end %>
<% else %>
    <% flash[:notice] = 'Create some comments for this Goal!' %>
<% end %>

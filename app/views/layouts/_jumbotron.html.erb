<div class="jumbotron">
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <h1><b><%= title %></b></h1>
      </div>
    </div>
    <%= yield %>
    <% if paginatable.present? %>
        <div class="row">
          <div class="col-xs-12">
            <%= paginate paginatable %>
          </div>
        </div>
    <% end %>
  </div>
</div>

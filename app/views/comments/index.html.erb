<%= content_for :title, t('comments.title.index') %>
<%= render layout: 'layouts/panel', locals: {title: 'All Comments', resource: @comments} do %>
    <%= render partial: 'comments', locals: {action: :index} %>
<% end %>

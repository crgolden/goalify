<%= content_for :page_title, t('goals.title.search') %>
<%= content_for :nav, render('form_search') %>
<% @goals.each do |goal| %>
    <%= render layout: 'layouts/list_group' do %>
        <%= render 'controls', goal: goal %>
        <%= render goal %>
    <% end %>
<% end %>

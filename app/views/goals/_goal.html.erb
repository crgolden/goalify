<p><%= goal.text %></p>
Goal title: <b><%= goal.title %><br></b>
Goal score: <%= link_to goal.score, scores_path(goal: goal), class: 'badge' %><br>
Goal User: <%= link_to goal.user.name, goal.user %> <%= link_to goal.user.score, scores_path(user: goal.user), class: 'badge' %>

<%= render layout: 'layouts/panel', locals: {title: 'Sign in', resource: nil} do %>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name),
                 html: {role: 'form', class: 'form-horizontal'}) do |f| %>
        <%= devise_error_messages! %>
        <div class="authform">
          <div class="field form-group">
            <%= f.label :email, class: 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.email_field :email, autofocus: true, class: 'form-control' %>
              <%- if devise_mapping.registerable? %>
                  <%= link_to 'Sign up', new_registration_path(resource_name), class: 'right' %>
              <% end -%>
            </div>
          </div>
          <div class="field form-group">
            <%= f.label :password, class: 'col-sm-2 control-label' %>
            <div class="col-sm-10">
              <%= f.password_field :password, class: 'form-control' %>
              <%- if devise_mapping.recoverable? %>
                  <%= link_to 'Forgot password?', new_password_path(resource_name), class: 'right' %>
              <% end -%>
            </div>
          </div>
          <div class="actions form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <%= f.submit 'Sign in', class: 'button right' %><br>
            </div>
            <% if devise_mapping.rememberable? -%>
                <div class="checkbox col-sm-offset-2 col-sm-10">
                  <label>
                    <%= f.check_box :remember_me %> Remember me
                  </label>
                </div>
            <% end -%>
          </div>
        </div>
    <% end %>
<% end %>

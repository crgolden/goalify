<%= content_for :title, t('comments.title.index') %>
<% if @comments.present? %>
    <%= render layout: 'layouts/panel', locals: {title: 'All Comments', resource: nil} do %>
        <%= render 'goals/comments' %>
    <% end %>
<% else %>
    <% flash[:notice] = 'Create some comments for this Goal!' %>
<% end %>

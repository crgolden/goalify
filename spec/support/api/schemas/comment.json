{
  "definitions": {
    "id": {
      "type": "string"
    },
    "goal_id": {
      "type": "string"
    },
    "user_id": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "created_at": {
      "type": "string",
      "format": "date-time"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    }
  },
  "type": "object",
  "required": [
    "comment"
  ],
  "properties": {
    "comment": {
      "type": "object",
      "required": [
        "id",
        "body",
        "goal_id",
        "user_id"
      ],
      "properties": {
        "id": {
          "$ref": "#/definitions/id"
        },
        "body": {
          "$ref": "#/definitions/body"
        },
        "goal_id": {
          "$ref": "#/definitions/goal_id"
        },
        "user_id": {
          "$ref": "#/definitions/user_id"
        }
      }
    }
  }
}